import type { Workspace } from "./types.js";

// General-purpose AI Learning Platform: No pre-coded or demo workspaces.
// Workspaces are created on demand by user request and persisted cleanly.
export const DEFAULT_WORKSPACES: Workspace[] = [];
